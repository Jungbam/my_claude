# QA 팀 통합 프로토콜 — 디자인/DS 감사

> **대상**: 프로젝트 UI/디자인 QA를 담당하는 팀원 모두
> **목적**: 팀 전체가 하나의 SSOT로 같은 기준을 사용해 디테일하게 QA를 수행한다.

---

## 0. 스코프 & 원칙

- **범위**: 디자인/DS + UX + 표기(SEC/DART 등 출처). **데이터 로직·기능 QA는 범위 밖** (다른 담당).
- **관점 제외**: 반응형/모바일은 이번 프로토콜 밖 (별도 트랙).
- **원칙**: "고쳤습니다" 대신 **숫자**로 답한다. 화면만 보지 말고 **재기**부터.
- **감사자 자세**: read-only 검토 우선 — 판정만 내리고 필요 시 수정 요청. 프로덕션 파일 직접 편집은 프리뷰 격리 후에만.

---

## 1. 3단계 프로세스 (전체 흐름)

**한 페이지 QA = 3단계를 순차/병렬로 통과**:

| 단계 | 방식 | 주체 | 결과 |
|------|------|------|------|
| **① DS 정합** | 자동 스크립트 (verify-* 11종) | 스크립트 | pass/fail |
| **② UX** | 화면 보고 사람 판단 | QA 담당 | 지시 목록 → 공통 컴포넌트 도출 |
| **③ 표기 검사** | 출처(SEC/DART) 기준 대조 | 사람 | 문구/서식명/통화 정정 |

3단계 전부 통과해야 "QA 완료". 각 단계 세부는 §2~§4.

---

## 2. ① DS 정합 — 자동 검증 (스크립트)

### 검증 스위트 (이관 전 통과 필수)

| 관점 | 스크립트 | 상태 |
|------|---------|------|
| raw 색상 리터럴 금지 | `verify-design-system` | 활성 |
| 우회 클래스 직접 사용 금지 (26종 지정) | `verify-design-system` | 활성 |
| 승인 안 된 인라인 style 금지 (allowlist) | `verify-design-system` | 활성 |
| `globals.css` UI 증분 금지 | `verify-design-system` | 활성 |
| `@base-ui/react` 금지 — radix 기준 | `verify-design-system` | 활성 |
| 페이지 컨테이너 하드코딩 금지 → `PageContainer` | `verify-design-system` | 활성 |
| 안 쓰는 토큰 · 토큰 소비 규약 | `verify-tokens-usage` | 활성 |
| 다크 블록이 raw hex 를 재복제하는지 | `verify-ax-tokens` | 활성 |
| 전역 CSS 클래스 오남용 | `audit:global-class` | 활성 |
| margin · `space-*` 하드코딩 | `verify-no-margin` | **비활성 (수동 검토)** |
| 원본 JSON 클라이언트 번들 누수 | `verify-bundle` | 활성 |

### 스크립트가 못 잡는 것 — 사람이 직접 확인

- 공통 컴포넌트로 되어 있나 (비슷한 UI는 무조건 DS)
- 의도된 차이인가 (맞으면 주석: 작업명·날짜·의도·SEC 영향)
- 읽히는가 (대비 — **실측 수치**로 기록)
- 글자 크기 · 줄바꿈 (14px floor · `keep-all` — 한국어)

### DS 세부 카테고리 (① 단계에서 함께 확인)

| 카테고리 | 확인 사항 | 위반 판정 근거 |
|----------|-----------|---------------|
| **A. 상단바** | `SCREEN NN · … · SEC` 장식 라벨 제거, source/form이 hero 배지에 이미 있으면 중복 제거. BackLink + 원문 버튼만 잔존 | 중복 배지 존재 |
| **C. 간격 리듬** | eyebrow→hero 16 / hero→본문 16 / 섹션 간 24. **margin 무사용(gap 기반)**. TickerHero `mb-4` → `mb-0` | margin 하드코딩 발견 시 |
| **E. 카드** | 섹션 = 공유 `SectionCard`(heading 슬롯). 내부 = `Card`/`StatCell`. 텍스트 값은 StatCell(mono·tabular=숫자용) 부적합 → 수제 유지 허용. 수제 카드의 arbitrary `[var(--…)]` 지양 | 공유 컴포넌트 미사용 (교체 가능 시) |
| **P. Pill/Badge** | 정적 라벨 = 공유 `Badge`. **인터랙티브 트리거(ⓘ·토글)는 Badge 부적합** → 별도 | 인터랙티브 요소가 Badge 사용 |
| **S. 공유 컴포넌트 교체** | back/원문 → `IconTextButton` / 접이식 → `AccordionSection` / 필터 탭 → `ChipTabbar` | 수제 대신 공유 소비 안 함 |
| **radius 위계** | 외곽 14(`--radius-card`) / 내부 10(`--radius-md`) / 칩 6(`--radius-sm`) | 위계 위반 |
| **인라인 style** | 금지. 동적 값(차트 막대 높이 등)만 `verify-design-system` allowlist 등록 | allowlist 밖 인라인 style |
| **row/리스트** | flat + `hover:bg` / 반복 라벨 제거 / 값 우측 끝 정렬 통일 | 정렬 불일치 |
| **차트** | 같은 페이지 내 시각 통일. SVG `preserveAspectRatio="none"` 텍스트 왜곡 주의(라벨은 HTML) | 시각 불통일 |
| **중복/저정보 배지·칩** | 제목 중복, 데이터 존재만으로 자명한 상태 등은 숨김 | 정보 중복 |
| **a11y (기본)** | 공유 컴포넌트 소비하면 대부분 자동. 수제 트리거만 `aria-label`, `focus-visible`, 한국어 `keep-all`, 14px floor 확인 | 수제 트리거 aria 누락 |

---

## 3. ② UX — 사람 판단 (지시 + 공통 컴포넌트 도출)

### 화면 보고 확인할 것

- 같은 기능인데 SEC와 DART가 다르게 보이나 (페이지네이션·목록 건수)
- 카드 안에 카드처럼 겹쳐 보이나 (변동 이력·경력)
- 구분이 필요한데 안 되어 있나 (배경·왼쪽 라인)
- 다른 페이지에서 쓰던 색·형태와 다른가
- 빠진 조작이 있나 (모달 닫기 버튼)
- 양이 많아 감당이 안 되나 (보고자 1,078명 · 거래 4,046건)

### 실제 나오는 지시 예시

```
"SEC랑 DART랑 페이지네이션이 다르네 — SEC 방식으로 통일하고 싶어"
"여기 카드 안에 또 카드야 — 왼쪽에 라인 있는 형태로 바꾸고 싶어"
"이게 많으니까 배경은 필요할 것 같아"
"라인색은 다른 페이지에 쓴 거랑 같은 걸로"
"모달 위에 닫기 버튼 있으면 좋겠어"
"직위 구분선은 안 넣고 가자"
"거긴 다른 곳이니까 손대지 마"
```

### 지시 → 공통 컴포넌트 도출

- 여러 곳에 반복되는 손코딩은 **공통 컴포넌트로 승격**
  - 예: 손코딩 모은 것 4종(카드·섹션·빈 상태·페이지네이션) + 새로 만든 것 3종(검색형 선택 창·페이지네이션·accent 블록)
- 신규 컴포넌트 즉시 추가 금지 → **승격 핸드오프 문서 후보 등록 → 결정**
- 기존 공유 컴포넌트 수정 = **사전 경고 + 명시 허락 필수**. 소비(슬롯/props)는 자유.

---

## 4. ③ 표기 검사 — 출처 기준 (SEC/DART 등)

같은 화면을 SEC·DART가 함께 쓰기 때문에, 한쪽 기준 문구가 그대로 남아 있는지 확인.

| 확인 항목 | 예시 |
|-----------|------|
| 서식명 | DART 인데 SEC 서식명(DEF 14A)으로 표기되고 있진 않나 |
| 통화 기호 | 데이터의 통화와 화면의 기호가 맞나 |
| 목록 설명 | 실제로 있는 탭을 안내하고 있나 |
| 링크 미리보기 | 출처별로 다른 설명이 나가고 있나 |
| 제도 기준 | "5% 이상 / 초과" 처럼 제도가 다른 표현 |

**③ 표기 검사는 자동화 안 됨** — 사람이 각 화면의 문구를 출처와 대조.

---

## 5. Phase 1 / Phase 2 시점 구분

두 Phase는 **동일한 §2~§4 기준을 공유**한다. 다른 것은 시점·범위·출력.

### Phase 1 — 페이지 리뉴얼 *중* 검토
- **시점**: 작업 중 반복 검토 (§7 순서 4번 반복)
- **범위**: 지금 손보는 페이지 하나
- **출력**: 위반/개선 findings — 반영 후 재검토

### Phase 2 — 최종 병합 *후* 감사 (final sweep)
- **시점**: 개발자가 여러 디자이너 작업을 master에 병합한 후
- **범위**: 페이지 **전체**
- **출력**: **비-DS 요소 스캔 → 분류**
  - (a) **교체 가능** — 대응 DS 있음 → 교체안 제시
  - (b) **신규 필요** — 대응 DS 없음 → 승격 후보로 분류 → 공유컴포넌트 승격 핸드오프 문서에 올려 결정

---

## 6. 프로세스 제약 (위반 = fail)

1. **프리뷰 격리** — 프로덕션 파일 직접 수정 금지 → throwaway `*-preview`에서 반복 → 검증 → 이관 → 삭제
2. **브랜치** — 페이지별 브랜치. main/master 직접 커밋·PR 금지
3. **lib/데이터 미접촉**
4. **공유 컴포넌트** — 수정 = 사전 경고 + 명시 허락 필수. 핸드오프 문서에 후보 등록. 소비(슬롯/props)는 자유
5. **신규 공유 컴포넌트** — 즉시 추가 금지 → 승격 핸드오프 문서에 후보 등록 후 결정
6. **목업** — 참고용만. 프로덕션 토큰 사용, 의도된 UI 차이는 코드 주석 명시

---

## 7. 한 페이지 고치는 순서

```
1  화면 보기      dev 서버 띄우고 현재 상태 확인
2  데이터 재기     몇 건인지 · 최대 / 중간값
3  문제 목록      고칠 것을 뽑아 문서로 정리
4  하나씩 고치기    손코딩이면 공통 컴포넌트로 · 공통에 없으면 새로 만들어 공유
                같은 컴포넌트를 SEC 도 쓰는지 매번 확인
5  다시 보기      화면 확인 · 안 바뀌어야 할 건 픽셀 대조
6  커밋          주석에 작업명 · 날짜 · 의도 · SEC 영향
```

**2번을 먼저 하는 이유**: 화면만 보면 규모를 모른다. 실제 사례로, 내부자거래 페이지는 보고자가 1,078명인 종목이 있어서 드롭다운을 검색형으로 바꿨고, 거래가 최대 4,046건이라 페이지당 50건으로 정했다. **재보기 전에는 둘 다 몰랐다.**

---

## 8. 브라우저 실측

저장소에 테스트 프레임워크 부재(vitest·jest·playwright 설정 없음). 새로 만들지 말고, 브라우저 띄워 **숫자로 재는 방식**.

| 무엇을 | 어떻게 |
|--------|--------|
| 시각이 안 바뀌었는지 | 수정 전후 캡처 → 픽셀 비교. 차이 0 이면 등가 |
| 크기·비율 | DevTools 계산값을 폭별로 측정 |
| 페이지 넘침 | 문서 폭 − 화면 폭 |
| 동작 | 클릭·키보드 입력 후 URL·화면 상태 확인 |

**답할 때는 숫자**: `차트 배율 7개 폭 전건 1.00` 처럼.

---

## 9. 의사결정 / 문서화

**무엇을 남기나** — 담당 QA 선에서 못 끝내는 것만. 끝낸 것은 커밋 메시지에 적는다.

| 분류 | 처리 |
|------|------|
| 혼자 결정 가능 | 그냥 함 → 커밋 메시지에 근거 |
| 남의 파일 · 공용 부품 | 문서 (승격 핸드오프) |
| 데이터 파이프라인 | 문서 |
| 방향 선택 필요 | 문서 (선택지 2~3개) |

### 문서 항목 하나의 구성

```
증상    화면에서 뭐가 잘못 보이는가 (숫자로)
원인    왜 그런가
변경안  어떻게 고칠 것인가 · 선택지가 있으면 2~3안
```

### 지키려는 것

| | |
|---|---|
| 숫자로 적기 | "많다" 대신 "1,078명" |
| 코드 안 넣기 | 표·그림으로 대체 |
| 내부 용어 안 쓰기 | 풀어서 |
| 선택지는 2~3개 | 하나만 제시 = 결정이 아니라 통보 |
| 틀린 진단은 정정 기록 | 실측으로 뒤집힌 판단은 과정을 남김 |

---

## 10. 파이프라인 (crew:ship)

`/crew:ship` 스킬 하나로 검증 → 커밋 → 푸시 → PR을 묶어 실행.

| 단계 | 하는 일 | 통과 못 하면 |
|------|---------|-------------|
| 기준 브랜치 머지 | 검증보다 먼저 master를 머지해 충돌을 미리 드러냄 | 중단 |
| 검증 게이트 | 1장의 스크립트 11종 순서대로 실행 | 중단 |
| 코드 리뷰 | `code-reviewer` 호출 → PASS/WARN/FAIL | FAIL 중단 |
| 커밋 | 자격증명 파일 스캔 후 커밋 | 검출 시 중단 |
| 재검증 | 리뷰 지적 반영 시 게이트 처음부터 다시 | 중단 |
| 푸시 | 사용자 확인 1회 | — |

**리뷰 반영으로 코드가 바뀌면 앞서 돌린 결과는 근거가 안 되므로 전부 다시 돌린다.**

게이트는 `package.json`에서 자동으로 찾는다. **병렬 실행 금지** — 출력이 섞이면 어느 게이트가 실패했는지 못 짚음.

---

## 11. 역할 매트릭스 (시점 × 단계)

| 시점 | ① DS 정합 | ② UX | ③ 표기 |
|------|-----------|------|-------|
| Phase 1 (작업 중) | 스크립트 자동 실행 | 담당 QA 화면 보고 지시 | 담당 QA 화면 보고 대조 |
| Phase 2 (병합 후) | 스크립트 재실행 + 비-DS 스캔 | 페이지 전체 UX 감사 | 전체 표기 재확인 |
| 결정 | 자동 | 담당 QA 선 or 공용/방향은 문서 | 담당 QA 선 or 서식 기준 문서 |

**감사자는 read-only 우선** — 판정만 내리고 필요 시 개발자에게 수정 요청. 프로덕션 파일 직접 편집은 프리뷰 격리(§6-1) 후에만.

---

## 12. 첫 도입 체크리스트

**신규 페이지·팀원이 이 프로토콜을 처음 적용할 때**:

- [ ] `verify-design-system` 등 11 스크립트 로컬 실행 가능한지 확인
- [ ] `PageContainer` / `SectionCard` / `Card` / `StatCell` / `Badge` / `IconTextButton` / `AccordionSection` / `ChipTabbar` 등 공유 컴포넌트 위치·props 숙지
- [ ] SEC vs DART 서식명·통화·제도 기준 차이 숙지 (③ 표기 검사)
- [ ] 페이지별 브랜치 만들고 시작 (§6-2)
- [ ] `*-preview` 격리 원칙 (§6-1) 이해
- [ ] `/crew:ship` 파이프라인 로컬 실행 확인
- [ ] "숫자로 답하기" 습관 (§8) 팀 규약으로 인지

---

**이 문서가 SSOT**. 향후 QA 기준 변경은 이 문서를 먼저 갱신 후 팀 공유.
